import 'package:flutter/material.dart';

class MyStateles extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new MaterialApp(
      home: Text('This is stateles widget'),
    );
  }
}
